# Solar Cooling Project

## Project Status
This project uses MkDocs for documentation.

### Implementation Plans
- **MkDocs Setup**:
  - [x] Install `mkdocs` and `mkdocs-material`
  - [x] Configure `mkdocs.yml`
  - [x] Structure `docs/` directory
  - [x] Migrate existing reports and plans to `docs/`

## Running the Docs
```bash
uv run mkdocs serve
```

### Running on a Different Port
To serve on a specific port (e.g., 8001):
```bash
uv run mkdocs serve -a localhost:8001
```
